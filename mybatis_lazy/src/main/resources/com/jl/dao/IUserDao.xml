<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jl.dao.IUserDao">
    <resultMap id="userMap" type="com.jl.bean.User"/>
    <!--定义user的resultMap-->
    <resultMap id="userAccountMap" type="com.jl.bean.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="address" column="address"/>
        <result property="sex" column="sex"/>
        <result property="age" column="age"/>
        <!--
            配置user对象中的account对象
        -->
        <collection property="account" ofType="com.jl.bean.Account">
            <id column="aid" property="id"/>
            <result property="uid" column="uid"/>
            <result property="money" column="money"/>
        </collection>
    </resultMap>
    <sql id="defaultSQL" >
        SELECT * FROM user
    </sql>

    <select id="findAll" resultMap="userAccountMap">
--         select * from user
        select  u.*,a.money from user u left join  account a on u.id = a.uid
    </select>

    <select id="findById" resultMap="userMap" parameterType="INT">
        select * from  user where id = #{id}
    </select>


</mapper>
